# 🌤️ Tenki Weather App

A weather app that shows current conditions and forecasts for cities around the world.

🔗 **Live:** [Add live URL if deployed]

## Features

- **Current weather** — temperature, humidity, wind speed
- **4-day forecast** — see what's coming up
- **Hourly breakdown** — today's weather by hour
- **Location detection** — use your current location or search manually
- **Dark mode** — toggle between light and dark themes
- **Unit switching** — toggle between °C/°F and metric/imperial

## Tech Stack

HTML · CSS · JavaScript · OpenWeatherMap API · REST Countries API

## Run Locally
```bash
git clone https://github.com/LeoCba07/tenki-app.git
```

Get an API key from [OpenWeatherMap](https://openweathermap.org/api) (free tier), then create `config.js`:
```js
export const API_KEY = 'your-api-key-here';
```

Open `index.html` in your browser.

## What I Learned

- Working with REST APIs and async/await
- JavaScript modules and project structure
- CSS animations and responsive design
- Error handling for edge cases
